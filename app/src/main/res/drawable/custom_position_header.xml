<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo de la corona -->

    <!-- Fondo de color sólido con borde -->
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="2px"
                android:color="#000000" /> <!-- Borde negro de 2 píxeles -->
            <solid android:color="#F4CE94" /> <!-- Color de fondo personalizado (por ejemplo, amarillo) -->
        </shape>
    </item>
    <item
        android:right="20dp"
        android:left="20dp"
        android:top="5dp"
        android:bottom="5dp"
        android:drawable="@drawable/header_crown" />

</layer-list>
